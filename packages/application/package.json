{
  "name": "@maestro/application",
  "version": "1.0.0",
  "private": true,
  "description": "Couche application — use cases et DTOs orchestrant le domaine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@maestro/domain": "workspace:*"
  }
}
